# Backtest Results

Not enough history yet to backtest - the longest horizon tested (20 snapshots) needs at least 21 distinct days, only 3 exist so far. This accumulates automatically every time FUNDAMENTALS SCAN runs (locally or in the daily cloud workflow) - just re-run `python backtest_signals.py` again in a few weeks, no other action needed.
